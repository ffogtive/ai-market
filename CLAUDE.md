# Working rules

## Hypothesis first (필수)
- 문제해결/조사/구현을 시작할 때, 본문보다 먼저 **핵심 가설을 1~3줄로** 맨 위에 적는다.
  - 형식: `가설: ...` / `검증 방법: ...` / (진행 후) `결과: 맞음|틀림 → 다음 가설`
- 목적: 가설 없이 세부 작업만 따라가다 산으로 가는 것 방지. 사용자가 최전방 가설을 한눈에 보고 틀렸으면 즉시 방향을 틀 수 있게 한다.
- 가설이 바뀌면 바뀐 가설을 다시 맨 위에 명시한다.

## 사용자는 비개발자 (필수)
- 내가 할 수 있는 일(머지, 브랜치 정리, 설정, 테스트)은 직접 한다. 사용자에게 웹 화면 조작이나 터미널 작업을 넘기지 않는다.
- 사용자 기기에서만 가능한 일은 불가피할 때만, **복붙 한 번**으로 끝나는 명령으로 준다. zsh에서 깨지는 `#` 주석은 넣지 않는다.
- 막혀서 못 하는 일은 무엇을 시도했고 왜 막혔는지 한 줄로 말하고, 사용자가 꼭 해야 할 때만 가장 짧은 방법을 준다.

## 에이전트·모델 배정 (필수)
- **'최소 토큰, 최대 효율' + 선택과 집중.** job 난이도에 맞는 모델을 쓰고, 오버스펙을 잡지 않는다.
  - 기계적 작업(스크립트 하나, 문서 갱신, 포맷·이름 정리, 단순 반복 수정) → **haiku**
  - 일반 구현·검증·테스트·버그 수정 → **sonnet**
  - **기획·정의·문제해결·설계 판단·통합 → 최대 모델 + 최대 추론.** 여기서는 아끼지 않는다.
- 기능의 **정의·설계는 상위 모델이 먼저 확정**하고, 그 결과를 받아 구현만 하위 모델에 넘긴다. 정의가 얇은 채로 구현 에이전트를 돌리지 않는다.
- 에이전트 프롬프트에 환경(파이썬 경로·테스트 명령·데이터 위치·금지 사항)을 미리 박아 탐색 비용을 없앤다. 비싼 호출(LLM 요약 등)은 횟수를 못 박는다.
