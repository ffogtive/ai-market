# Working rules

## Hypothesis first (필수)
- 문제해결/조사/구현을 시작할 때, 본문보다 먼저 **핵심 가설을 1~3줄로** 맨 위에 적는다.
  - 형식: `가설: ...` / `검증 방법: ...` / (진행 후) `결과: 맞음|틀림 → 다음 가설`
- 목적: 가설 없이 세부 작업만 따라가다 산으로 가는 것 방지. 사용자가 최전방 가설을 한눈에 보고 틀렸으면 즉시 방향을 틀 수 있게 한다.
- 가설이 바뀌면 바뀐 가설을 다시 맨 위에 명시한다.

## 사용자는 비개발자 (필수)
- 내가 할 수 있는 일(머지, 브랜치 정리, 설정, 테스트)은 직접 한다. 사용자에게 웹 화면 조작이나 터미널 작업을 넘기지 않는다.
- 사용자 기기에서만 가능한 일은 불가피할 때만, **복붙 한 번**으로 끝나는 명령으로 준다. zsh에서 깨지는 `#` 주석은 넣지 않는다.
- 막혀서 못 하는 일은 무엇을 시도했고 왜 막혔는지 한 줄로 말하고, 사용자가 꼭 해야 할 때만 가장 짧은 방법을 준다.
